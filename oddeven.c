#include<stdio.h>
int main()


{
    int len,width,area;
    printf(" Enter  lenght and width simulteniously \n ");
    scanf(" %d %d",&len,&width);

    area = (len*width);

    printf(" Area of Tringle = %d ",area);

    return 0;
}